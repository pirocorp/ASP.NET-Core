@model CarFormModel

@{
    this.ViewData["Title"] = "Add Car";
}

<h2>@(this.ViewData["Title"])</h2>

<div class="row">
    <div class="col-sm-4">
        @(this.Html.Partial("_CarForm", this.Model))
    </div>
</div>